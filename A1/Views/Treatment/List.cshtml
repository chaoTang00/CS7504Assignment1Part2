@model A1.ViewModels.TreatmentListViewModel

<h1>@Model.CurrentPromotion</h1>

<div class="row row-cols-1 row-cols-md-3 g-4">

    @foreach (var treatment in Model.Treatments)
    {
        <partial name="_TreatmentCard" model="treatment" />
    }

</div>